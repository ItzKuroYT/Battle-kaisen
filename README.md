# Battle Kaisen

Static client runs on GitHub Pages.

For global multiplayer, deploy the WebSocket server in [ws-server](ws-server) and set the WebSocket URL in the in-game Options menu.

- Client hosting: GitHub Pages
- Matchmaking/relay hosting: Node host (Render/Railway/Fly)
